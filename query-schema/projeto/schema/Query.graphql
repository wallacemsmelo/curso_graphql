#---------------------------------------
# CONSULTA - Insiro aqui os Dados que quero Obter
#--------------------------------------
type Query { # O nome dos atributos devem ser os mesmos que estão em resolvers
  ola: String!
  horaCerta: String
  usuarioLogado: Usuario
  produtoEmDestaque: Produto
  numerosMegaSena: [Int!]!
  usuarios: [Usuario]
  usuario(id: Int): Usuario
  perfis: [Perfil]
  perfil(id: Int): Perfil
}
